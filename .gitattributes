# Normalize line endings to LF on checkout everywhere, so Windows CI does not
# see LF-generated files (e.g. client/src/generated/product.ts) as "stale" CRLF.
* text=auto eol=lf

# Windows-native scripts keep CRLF.
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf
